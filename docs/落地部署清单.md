# 小范围落地部署清单

**项目：** 在线订餐系统  
**文档日期：** 2026年3月26日（最后更新：2026-03-26）  
**目标：** 完成小范围真实商业化落地所需的全部工作项  
**当前服务器：** `43.155.34.32:5000`（腾讯云，TencentOS 4，2核2G）

---

## 优先级说明

| 级别 | 含义 |
| :--- | :--- |
| 🔴 P0 | 上线前必须完成，否则无法安全运行 |
| 🟡 P1 | 上线后尽快补充，影响基本用户体验 |
| 🟢 P2 | 稳定运行后优化，提升体验，非阻塞上线 |

---

## 🔴 P0 — 上线前必须完成

### 一、服务器 & 网络

- [x] 购买云服务器：腾讯云 2核2G，TencentOS 4，公网 IP `43.155.34.32`（2026-03-26 完成）✅
- [x] 绑定域名，完成 DNS A 记录解析到服务器 IP ✅（2026-03-26 完成：`beautyteam.sztu1881.com` A 记录 → `43.155.34.32`）
- [ ] 申请 SSL 证书（Let's Encrypt 免费，Certbot 自动续期）⏳ 待服务器执行 Certbot 命令
- [x] 配置并启用 Nginx 反向代理 ✅（2026-03-26 完成）：
  - HTTPS 版本配置已就绪：`nginx/nginx.conf` 已升级，80 端口处理 ACME 验证并 301 跳转，443 端口 SSL + 反向代理
  - `docker-compose.yml` 已取消 443 端口和证书挂载注释
  - 待服务器执行 Certbot 申请证书后即可生效
- [x] 服务器级防火墙（firewalld）已配置：只开放 ssh/http/https，关闭 5000/3307 等内部端口 ✅（2026-03-26 完成，`firewall-cmd --list-all` 确认）
- [ ] **腾讯云安全组**：在腾讯云控制台同步关闭 5000/3307 端口（安全组是云平台层面防护，需在控制台操作）

### 二、安全加固

- [x] 修改 `docker-compose.yml` 中所有弱密码：
  - MySQL 密码已改为读取 `.env` 变量，本地默认值仅限开发 ✅
  - Redis 已增加 `REDIS_PASSWORD` 环境变量支持，生产环境设置后自动启用 `requirepass` ✅
- [x] 创建生产环境 `.env` 模板（`.env.example` 已提交，服务器上 `cp .env.example .env` 后填写真实值）✅
- [x] 确认 `.env` 已加入 `.gitignore`，`.env.production` 也已加入忽略 ✅
- [x] **BUG-09 CSRF 防护**：已安装 `Flask-WTF==1.2.1`，全部 Jinja 表单加 `csrf_token()` 隐藏字段，所有 AJAX fetch/jQuery 请求自动注入 `X-CSRFToken` 头，`/api/v1` 蓝图已排除（JWT 鉴权无需 CSRF）✅（服务器实测：无 Token 的 POST 返回 400，API 正常返回 401）
- [x] 登录失败限流已验证：服务器实测第 11 次登录请求返回 HTTP 429，Redis 后端正常工作 ✅（`RATELIMIT_STORAGE_URI` 已修复，登录路由加了 `@limiter.limit("10 per minute")`）

### 三、数据库

- [x] 确认 `docker-compose.yml` MySQL 数据目录已挂载到宿主机 volume（重启容器不丢数据）✅（第二次启动时日志显示"数据库已有数据，跳过初始化"，volume 持久化正常）
- [x] 首次部署前确认数据库初始化脚本正常运行（`entrypoint.sh` 中 `db.create_all()`）✅（日志确认"数据库表创建成功！测试数据初始化成功！"）
- [x] 配置数据库**自动定时备份**：`crontab` 每天凌晨 3 点执行 `mysqldump`，备份到 `/opt/backups/mysql/`，保留最近 14 天 ✅（2026-03-26 实测备份成功，文件 `db_20260326_180612.sql.gz`）
- [ ] 备份远程上传：配置 `rsync` 或腾讯云 COS CLI 将备份文件同步到对象存储（本地磁盘损坏时数据不丢失）
- [ ] 准备数据恢复演练方案（确认备份文件能正常还原）

---

## 🟡 P1 — 上线后尽快补充

### 四、高德地图 API 集成（已调整为 P1）

- [ ] 在[高德开放平台](https://lbs.amap.com/)注册账号，申请 Web 端 JS API Key（免费额度：每日 5000 次，足够小规模使用）
- [ ] 前端集成高德地图 JS SDK：用户进入首页时请求定位权限，获取经纬度
- [ ] 商家录入时保存地址坐标（经纬度），可通过高德地理编码 API 由地址自动解析
- [ ] 恢复"距离排序"功能：后端接收用户坐标，按 Haversine 公式计算各餐厅距离后排序
- [ ] 餐厅详情页显示地图定位标记

### 五、运营数据准备

- [ ] 修改种子数据脚本，初始化真实餐厅信息（名称、地址、营业时间、封面图）
- [ ] 录入菜品数据（名称、价格、描述、图片、分类）
- [ ] 初始化正式管理员账号，删除或修改测试账号密码
- [ ] 完整流程验收测试：注册 → 浏览餐厅 → 加入购物车 → 提交订单 → 商家接单 → 完成

### 六、功能补全

- [x] **评价提交入口**：已实现完整评价功能 ✅
  - 订单完成后在订单列表/详情页显示「写评价」按钮
  - 1-5 星评分 + 可选文字内容，防止重复评价
  - 提交后自动加权平均更新餐厅 `rating` 和 `review_count`
  - 首页平均评分从硬编码 4.8 改为动态计算（无真实评价时显示"暂无评分"）
- [x] **支付占位 UI**：订单详情页已展示微信支付 + 支付宝两个灰色占位按钮（标注"资质申请中"），附联系商家电话说明；等微信/支付宝商家 API 资质审批通过后，直接替换为真实支付 SDK 调用即可 ✅
- [ ] 图片上传持久化：确认 `app/static/images/` 目录挂载到宿主机 volume，容器重启后上传图片不丢失

### 七、监控 & 告警

- [ ] 配置 Gunicorn 日志输出（access log + error log）到宿主机挂载目录
- [x] 健康检查接口 `/health` 已实现并验证：服务器返回 `{"cache":"ok","db":"ok","status":"ok","timestamp":...}`，DB/Redis 异常时返回 503 ✅（2026-03-26 服务器实测通过）
- [ ] 部署健康检查告警脚本（服务器上配置 crontab，每 5 分钟 curl /health，失败发邮件/企业微信通知）
- [ ] 查看 Celery Worker 日志，确认异步任务（新订单通知、定时清理过期订单）正常运行

---

## 🟢 P2 — 稳定后可优化

### 八、支付 API 正式接入

> 订单详情页已有微信支付 + 支付宝占位 UI，资质到位后按以下步骤替换即可。

- [ ] 申请微信支付商家资质（需营业执照，微信支付官网提交）
- [ ] 申请支付宝商家资质（蚂蚁开放平台提交）
- [ ] 后端接入微信支付 SDK（`wechatpayv3`）/ 支付宝 SDK（`alipay-sdk-python`），替换占位按钮的禁用状态
- [ ] 实现支付回调接口（`notify_url`），验签后自动将订单状态更新为已付款
- [ ] 实现退款接口，关联到订单取消流程

### 九、通知能力

- [ ] 接入短信服务（阿里云 SMS / 腾讯云 SMS）：下单成功、状态变更时短信通知用户手机
- [ ] 商家端新订单推送：接入企业微信 / 飞书 Webhook，商家收到新订单时自动推送消息

### 十、测试覆盖率提升

- [ ] 当前整体覆盖率 60%，补充 `dish.py` / `category.py` / `cart.py` 页面路由测试
- [ ] 目标：上线前达到 **70%+**

### 十一、性能优化（并发提升时按需实施）

- [ ] Nginx 配置静态资源缓存（`Cache-Control`）
- [ ] 数据库慢查询日志开启，识别热点查询后加索引
- [ ] 考虑接入 CDN（图片资源加速）
- [ ] 超过 5000 并发时评估是否引入读写分离（MySQL 主从）

---

## 实施路径建议

```
阶段一（1–2 周）        阶段二（3–4 周）        阶段三（长期）
─────────────────      ─────────────────       ─────────────────
完成所有 P0 项目  →     完成 P1 核心功能  →     逐步推进 P2 优化
云服务器 + HTTPS         高德地图集成
安全配置 + 密码          评价功能
数据库备份               真实餐厅数据
CSRF 防护                监控告警
```

---

## 最关键的三件事

> 如果资源有限，优先确保以下三项：

1. **Nginx + HTTPS** — 没有它，域名无法访问、用户数据裸传
2. **改掉弱密码 + 配好 .env** — 否则数据库可能被公网扫描攻击
3. **数据库定时备份** — 用户数据和订单数据是最不可替代的资产

---

---

## 服务器部署记录

### 首次部署（2026-03-26）

| 项目 | 状态 | 备注 |
| :--- | :---: | :--- |
| Docker 安装（TencentOS 4） | ✅ | 使用阿里云镜像源，手动设置 releasever=9 |
| docker-compose 所有服务启动 | ✅ | web / mysql / redis / celery_worker / celery_beat 全部 healthy |
| `.env` 文件生成 | ✅ | 含随机 SECRET_KEY、MySQL 密码、Redis 密码 |
| MySQL 数据初始化 | ✅ | 表结构创建、测试种子数据写入 |
| `/health` 接口验证 | ✅ | `{"cache":"ok","db":"ok","status":"ok"}` |
| 首页访问 | ✅ | 返回 302 跳转登录页，正常 |

### 部署进度（服务器侧）

| 日期 | 操作 | 状态 |
| :--- | :--- | :---: |
| 2026-03-26 | 服务器购买、Docker 安装、代码部署、所有容器启动 | ✅ |
| 2026-03-26 | `.env` 生成强密码，健康检查 `/health` 验证通过 | ✅ |
| 2026-03-26 | firewalld 防火墙配置，仅开放 ssh/http/https | ✅ |
| 2026-03-26 | MySQL 每日凌晨自动备份（crontab + mysqldump） | ✅ |
| 2026-03-26 | 登录限流修复（Redis 后端，10次/分钟触发 429） | ✅ |

### 待办（服务器侧）

- [ ] **腾讯云控制台安全组**：关闭 5000 / 3307 端口（需手动进入控制台操作）
- [x] 绑定域名（`beautyteam.sztu1881.com` A 记录已添加）✅
- [ ] 申请 Let's Encrypt SSL 证书（Certbot 命令待执行）
- [x] Nginx 配置已升级为 HTTPS 版本（`nginx.conf` + `docker-compose.yml` 已更新）✅
- [ ] 备份远程同步到腾讯云 COS（当前备份仅保存在本机）

---

*文档由开发团队维护，每完成一项请勾选对应复选框并注明完成日期。*
